{
  "name": "docs-agent-verify-contract-fail",
  "description": "docs.agent_verify_contract fail: a command under Setup lacks a local verification instruction.",
  "detect": {"project_type": "service"},
  "files": {
    "pyproject.toml": "[project]\nname=\"svc\"\ndependencies=[\"fastapi\",\"uvicorn\"]\n",
    "app/main.py": "x = 1\n",
    "AGENTS.md": "## Setup\n\n```sh\nmake check\n```\n"
  },
  "expected": {"docs.agent_verify_contract": "fail"}
}
